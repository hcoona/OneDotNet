<?xml version="1.0" encoding="utf-8" ?>
<Project>
  <PropertyGroup>
    <EnlistmentRoot>$(MSBuildThisFileDirectory)</EnlistmentRoot>

    <NetfxLegacyTargetFramework>net45</NetfxLegacyTargetFramework>
    <NetfxCurrentTargetFramework>net48</NetfxCurrentTargetFramework>
    <NetcoreCurrentTargetFramework>netcoreapp3.0</NetcoreCurrentTargetFramework>
    <NetstandardCurrentTargetFramework>netstandard2.0</NetstandardCurrentTargetFramework>

    <DefaultExeTargetFrameworks Condition="!$([MSBuild]::IsOSUnixLike())">$(NetcoreCurrentTargetFramework);$(NetfxCurrentTargetFramework)</DefaultExeTargetFrameworks>
    <DefaultExeTargetFrameworks Condition="$([MSBuild]::IsOSUnixLike())">$(NetcoreCurrentTargetFramework)</DefaultExeTargetFrameworks>

    <DefaultLibraryTargetFrameworks Condition="!$([MSBuild]::IsOSUnixLike())">$(NetfxLegacyTargetFramework);$(NetstandardCurrentTargetFramework)</DefaultLibraryTargetFrameworks>
    <DefaultLibraryTargetFrameworks Condition="$([MSBuild]::IsOSUnixLike())">$(NetstandardCurrentTargetFramework)</DefaultLibraryTargetFrameworks>

    <Deterministic>true</Deterministic>
    <NoWarn>NU5048</NoWarn>
  </PropertyGroup>

  <PropertyGroup Condition=" '$(Configuration)' == 'DebugNonWindows' ">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>portable</DebugType>
    <Optimize>false</Optimize>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>

  <Import Project="$(EnlistmentRoot)/version.props" />
  <Import Project="$(EnlistmentRoot)/stylecop.props" />
</Project>
